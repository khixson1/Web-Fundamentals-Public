// The function when called will change the displayed 
//text in our button to 'Logout'
function switchText(element) {
    element.innerText = "Logout";
}

// The function when called will remove the displayed 
//button from our webpage
function removeButton(element) {
    element.remove();
}
